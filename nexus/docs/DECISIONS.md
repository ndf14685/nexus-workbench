# Nexus Workbench — Registro de decisiones

> Decisiones mayores en `adr/`. Este archivo registra las menores/operativas.

| # | Fecha | Decisión | Justificación |
|---|---|---|---|
| D-001 | 2026-07-28 | Docs propias en `nexus/docs/`, no en `docs/` | `docs/` es el Docusaurus de Wave (`onBrokenLinks: throw`); archivos ajenos rompen su build y generan conflictos de merge |
| D-002 | 2026-07-28 | Config propia en YAML bajo `nexus/config/`, proyectada a `connections.json`/`presets.json` | pedido explícito de config versionable fuera de SQLite; los JSON de Wave son API pública con watcher en caliente |
| D-003 | 2026-07-28 | Distinción de ambientes vía `term:theme` por conexión + presets `bg@nexus-*` | cero código UI nuevo, cero drift vs upstream; el indicador propio queda para Fase 2 |
| D-004 | 2026-07-28 | `publish.url` → `https://updates.nexus-workbench.invalid/releases` | TLD `.invalid` (RFC 2606) garantiza que el updater empaquetado jamás resuelva el feed de Wave; reversible cuando exista feed propio |
| D-005 | 2026-07-28 | Defaults del fork: `autoupdate:enabled=false`, `telemetry:enabled=false` | evitar updates no gobernados y no contaminar la telemetría de Wave con datos del fork |
| D-006 | 2026-07-28 | Guardas `repository_owner == 'wavetermdev'` en publish-release, bump-version, testdriver* | esos workflows requieren secretos de Command Line Inc y son ruido/riesgo en el fork; guardar > borrar (merge-friendly) |
| D-007 | 2026-07-28 | Identificadores internos (`appId`, `WAVETERM_*`, `~/.waveterm`, binarios) NO se renombran | son el motor (ADR-0001); renombrarlos rompe wsh remoto instalado, data dirs y el handshake WAVESRV-ESTART sin valor de producto |
| D-008 | 2026-07-28 | Fix del baseline: `tsgenevent_test.go` (assert multilínea) y mocks de `frontend/preview/` | rotos en upstream c99022c1; sin el fix no hay CI verde; si upstream los arregla, en el merge gana upstream |
| D-009 | 2026-07-28 | Tags de release del fork: `nexus-v*` (no `v*`) | los tags `v*` disparan `build-helper.yml` de upstream (firma/S3); separación limpia de canales |
| D-010 | 2026-07-28 | Toolchain local en `~/.local/nexus-toolchain/` (Go 1.25.12, Task 3.52) | la máquina no tenía Go/Task; instalación sin sudo, reproducible por bootstrap.sh |
| D-011 | 2026-07-28 | Baseline con 1 fallo de test Go y 12 errores tsc documentados y corregidos | honestidad del baseline: los fallos eran de upstream, no del fork |
| D-012 | 2026-07-28 | Versión de la app se mantiene = upstream (0.14.5) hasta la primera release propia | simplifica el primer sync; el canal nexus-v* independiza el versionado cuando haga falta |
| D-013 | 2026-07-29 | Feed de updates = GitHub Releases del fork (provider `github`); draft=candidate, publicada=stable | reemplaza el placeholder `.invalid` de D-004; electron-updater ignora drafts ⇒ compuerta manual sin infra propia |
| D-014 | 2026-07-29 | Tags de release del fork pasan a `v*` (reemplaza D-009); `build-helper.yml` guardado con repository_owner | el provider github de electron-updater espera tags `v<semver>`; la guarda elimina el riesgo que motivaba nexus-v* |
| D-015 | 2026-07-29 | AI: modos propios en `waveai.json` con keys en el secret store (`wsh secret` + `ai:apitokensecretname`); nunca tokens en config/repo | backends nativos anthropic/openai/gemini/ollama verificados en `pkg/aiusechat`; secret store cifrado vía safeStorage |
| D-016 | 2026-07-29 | Indicador de ambiente en tab bar: setting `nexus:environments` (SettingsType) proyectado por el importador a settings.json; componente propio en `frontend/app/nexus/` con inserción de 2 líneas en tabbar.tsx | cierra el diferido de D-003; el catálogo viaja por el config system nativo (watcher en caliente, tipos generados) y el diff en el árbol de Wave queda mínimo |
| D-017 | 2026-07-29 | verify.sh: check de branding valida `owner: "ndf14685"` y ausencia de `waveterm.dev` en electron-builder.config.cjs (antes exigía el placeholder `.invalid`) | el check había quedado obsoleto tras D-013 y fallaba en verde legítimo |
| D-018 | 2026-07-30 | Workspaces declarativos: RPC `WorkspaceCreateCommand` (wshserver) + `wsh workspace create --json` + `import-workspaces.mjs`; layout plano (fila de bloques), idempotente por nombre | única extensión RPC del fork hasta ahora; el layout plano cubre el catálogo real y el wire format admite enriquecerse; al aplicar layout propio se borran los bloques del new-tab default (ClearTree no toca la block list) |
| D-019 | 2026-07-30 | Las 40 alertas CodeQL iniciales (todas en código upstream) se descartan con justificación por clase; CodeQL sigue activo y las alertas nuevas se triagean una a una (tabla y regla en SECURITY.md) | reescribir el manejo de archivos/exec del motor rompería funcionalidad y violaría el drift mínimo (ADR-0001) sin eliminar riesgo real: app local single-user, RPC autenticado, sin límite de privilegio que cruzar |
| D-020 | 2026-07-30 | Sidebar izquierda de ambientes (estilo WinSSHterm): componente propio en `frontend/app/nexus/envsidebar.tsx` fuera del PanelGroup del workspace (div px + drag propio), catálogo desde `nexus:environments`, grupos por `kind` (Local/Servidores/Otros), estado por `getConnStatusAtom`, apertura vía `createBlock`/`refocusNode` (sin fuente de estado nueva); persistencia `nexus:sidebar*` en settings; la barra derecha filtra `nexus-env-*` cuando la sidebar está activa | sidebar = ambientes, tabs = sesiones; integrarla dentro del PanelGroup rompería la matemática del WorkspaceLayoutModel; los widgets de agentes/comandos NO son ambientes y quedan a la derecha |
| D-021 | 2026-07-30 | Wave AI sin protagonismo: con `app:hideaibutton=true` (default del fork) el panel queda DESMONTADO (no solo invisible), no se restaura de sesiones previas y `Cmd:Shift:A` no lo abre; capa neutral documentada en AI_PROVIDERS.md con la sección `agents` del catálogo como registro de adaptadores CLI v0 | el mount del panel llamaba al cloud de Wave (`ensureRateLimitSet`) aun invisible; los CLIs propios (Claude/Codex/Gemini/Ollama) con OAuth propio son la arquitectura de IA del Workbench, Wave AI queda como proveedor opcional reversible sin rebuild |
| D-022 | 2026-07-30 | Spatial Workspace (ADR-0006): el módulo (block) es la unidad, la ventana es superficie; paquete de diseño completo en `nexus/docs/spatial/` (arquitectura, modelo de datos, contratos, riesgos, migración, pruebas, MVP+backlog); engine backend-first en `pkg/spatial` + ventanas detached patrón Builder + evento WPS único `spatial:update` con bus tipado local | evolución a workstation multi-monitor sin reescribir el core y con camino a XR reservado solo en el modelo; el análisis del motor confirmó que ptys/estado ya viven fuera del componente visual y que la cola PendingBackendActions es el transporte correcto para mover módulos sin duplicarlos |
| D-023 | 2026-07-31 | Spatial MVP implementado (Tasks 1-12 de PLAN_MVP): catálogo de monitores propiedad de Electron y empujado al engine (`SpatialUpdateMonitorsCommand` → cache + diff connected/disconnected); cerrar la ventana detached con la X del SO = Pop In (el cierre real de un detached va por el engine serializado vía `SpatialCloseModuleCommand` con marca engineClosing); evento `module.minimized` como extensión al contrato §2; perfiles con lista blanca campo-a-campo (`buildProfileModule`, nunca `block.Meta` entero) en `<configdir>/nexus-profiles/`; `SpatialModel` arrancado en `initWave` (no solo en `initSurface`) | el engine debe quedar libre de Electron (los displays solo existen en emain) pero ser la única autoridad de mutación; "cerrar = Pop In" evita perder módulos por reflejo de ventana; `module.moved` implicaría bounds para minimize; serializar `block.Meta` filtraría `cmd:env`/tokens (test de whitelist lo bloquea); sin el arranque en initWave los guards R1 y el menú espacial de la ventana principal quedaban ciegos |
| D-024 | 2026-07-31 | Un cerebro, N shells (ADR-0007): el bloque Jarvis pasa de mock a vista del cerebro real vía `HttpJarvisRuntime` contra jarvisd :8770 (`nexus:jarvisbrainurl`/`nexus:jarvisbraintoken`, bearer); mock como fallback etiquetado "PREVIEW · simulado" y estado explícito "cerebro no conectado"; polling 2000 ms transicional hasta eventos push (M2); el Workbench no persiste estado de Jarvis | ejecuta el swap de 2 líneas previsto en ADR-0005 y paga su deuda de honestidad de superficie; token en settings es interino (destino: secret store D-015); aprobaciones = confirmación conversacional del cerebro (needs_confirmation + "sí"/"no" mismo source), cancelación solo /llm/cancel — lo no soportado se muestra como no soportado, nunca éxito simulado |
| D-025 | 2026-07-31 | M2 de ADR-0007 (lado cliente): `HttpJarvisRuntime` consume el canal SSE `GET /events` del Jarvis Protocol v1.1 (fetch+ReadableStream con parser puro, backoff 1/2/5/10 s, replay `?since=`, watchdog 45 s), se registra con `POST /clients/register` (client_id `wb-*`, capabilities `workspace.*` de la fachada espacial; moduleid = blockId o tipo amigable resuelto en el tab activo) y ejecuta `capability.invoke` respondiendo siempre `POST /capability/result`; el polling 2000 ms queda SOLO como fallback ante cerebros pre-v1.1 (404/501 en register/events) | mata el polling transicional de D-024 sin perder degradación elegante; EventSource no permite header Authorization, de ahí fetch+stream; el alambre snake_case es del contrato del cerebro (la regla lowercase-JSON del repo aplica a tipos de Wave, no al protocolo externo); la gobernanza de invocaciones queda en el cerebro (PEP `client.workbench.*`) |
| D-026 | 2026-07-31 | Auto-update real: `autoupdate:enabled=true` por defecto (supersede la mitad de autoupdate de D-005; telemetría sigue off), `publish.channel="beta"` + `autoUpdater.allowPrerelease` en `emain/updater.ts`, `win.verifyUpdateCodeSignature=false`, y en CI los tags `v*-beta.N` publican PRERELEASE automática mientras `vX.Y.Z` sigue dejando BORRADOR | D-005 apagaba el updater porque el feed era el de Wave; con el feed propio (D-013: owner/repo del fork, releases gobernadas por nosotros) apagarlo solo obliga al usuario a reinstalar a mano en cada beta. El canal debe ser `beta` y no `latest`: el provider github de electron-updater, con `allowPrerelease`, no empareja NINGUNA entrada del feed atom cuando el canal es `latest` (`shouldFetchVersion` exige null/alpha/beta) — y `beta` acepta además las stables publicadas, así que es superset. `verifyUpdateCodeSignature=false` porque el fork se distribuye sin firmar (D-013) y el updater rechazaría cada NSIS descargado. La compuerta manual de D-013/D-014 no se pierde: sigue viva exactamente donde importa, el canal stable |
| D-027 | 2026-07-31 | Cerebro Jarvis con configuración cero y sin reinicio (ADR-0007 addendum M3): default `http://127.0.0.1:8770` cuando `nexus:jarvisbrainurl` está vacía, fallback a las env vars `NEXUS_DESKTOP_BRAIN_URL`/`NEXUS_BRAIN_TOKEN` vía API angosta `getJarvisBrainEnv()` en emain, resolución pura en `jarvis-brain-config.ts` (setting > env > default), nuevo setting `nexus:jarvisbrainenabled` (`*bool`, default true) como única puerta al mock, y `JarvisCore.applyBrainConfig()` que reemplaza el runtime en caliente suscrito a las 3 claves de settings | el bloque exigía escribir un setting y reiniciar la app en cada instalación, cuando el caso abrumadoramente común es jarvisd en la misma máquina (sin token bindea loopback y no pide auth): el default lo cubre sin configurar nada. La API de ambiente es de lista blanca de 2 variables y no un getter genérico, que sería un olor de seguridad (el renderer barriendo el env del main). `*bool` y no `bool` porque `omitempty` se comería el `false` al serializar hacia el frontend y el opt-out quedaría inoperante. La sonda `/health` mantiene la honestidad de ADR-0007 §5: cerebro inalcanzable = "no conectado" con Reintentar, jamás degradación silenciosa al mock |
| D-028 | 2026-07-31 | UI del MVP: **una herramienta = un botón** — los `agents` del catálogo declaran `modes` (`{id,label,command,danger}`) y el importador genera UN widget `nexus-agent-<id>` con `blockdef.meta["nexus:modes"]`; con 2+ modos el click abre menú nativo (`showContextMenu`) en vez de crear el bloque, y los `commands` que dupliquen un agente (mismo id o mismo comando) se descartan con aviso. Además: se elimina la generación de `nexus-jarvis` del importador (el default `defwidget@jarvis` viaja con la app) dejando la clave en la limpieza para borrar duplicados viejos; `web:defaulturl` pasa de la URL de GitHub de Wave a `about:blank` (con `about` agregado a la lista de esquemas de `ensureUrlScheme`); `defwidget@processviewer` pasa a `display:hidden` y `web` queda último en la barra | el usuario reportaba dos botones de Claude, dos de Codex y dos de Jarvis: la causa no era el catálogo mal cargado sino el diseño del importador (una variante = un widget) y la duplicación default/generado de Jarvis. Los modos matan la duplicación en el origen y la guarda la hace mecánica. `about:blank` requiere el token en `ensureUrlScheme` porque su lista era `http\|https\|file` y todo lo demás cae en búsqueda (abría una búsqueda de Google por el literal "about:blank"); `""` se descartó porque Electron no crea el guest con src vacío y la barra de URL rompe al Enter. `display:hidden` NO deja el widget accesible desde el launcher (también filtra por esa clave): queda por `wsh launch defwidget@processviewer`, y todo el pase de minimalismo es reversible editando `widgets.json` |
| D-029 | 2026-08-01 | **Un click = terminal conectada, sin prompts intermedios (paridad WinSSHterm).** (a) En la sidebar de ambientes el click ÚNICO abre y asegura la conexión (`planEnvOpen`: crear / reenfocar / reenfocar+reconectar), no solo selecciona; (b) los nombres de conexión se canonizan en frontend e importador replicando `ParseOpts` + `SSHOpts.String()` (se omite `:22`, NO se toca el case); (c) el importador escribe identidad explícita (`ssh:user`/`ssh:port`/`ssh:identityfile`, `conn:wshenabled:false` con `wsh:false`) y aborta si un `ssh` no tiene `host` o un `wsl` no tiene `distro`; (d) `conn:askbeforewshinstall` pasa a `false` por defecto en el fork; (e) el fallo de arranque de shell se escribe SIEMPRE en la terminal vía `blocklogger.WriteErrorf`, sin depender de `term:conndebug`; (f) el overlay de estado muestra el motivo del error en cualquier ancho de bloque. **El prompt de host key desconocida SE MANTIENE**: es el único prompt que queda en la primera conexión, y es a propósito | El bug reportado ("hago click en un servidor y falla") tenía cuatro causas encadenadas y ninguna era SSH: el click no abría nada (`selectEnv` solo escribía `nexus:lastselectedenv`); cuando abría, un bloque sobre una conexión en `Status_Error` se reenfocaba sin reintentar porque `EnsureConnection` cachea el error para siempre y solo `ConnConnectCommand` lo limpia; el "Conectar" del menú usaba el timeout RPC default de 5s, que toda conexión real supera, y el error se perdía en `fireAndForget`; y en Windows, sin `User` en `~/.ssh/config`, `sshclient.go` manda `user.Current()` = `MAQUINA\usuario` como login. Encima, dos modales (host key + instalación de wsh) consumían el mismo presupuesto de 60s del ConnEnsure: un humano lento producía `context deadline exceeded` y con eso el error pegajoso. `askbeforewshinstall=false` es seguro porque el fallo de instalación NO es fatal (`tryEnableWsh` devuelve un `NoWshReason` y la conexión degrada a shell plana), y quien no quiera wsh en un host tiene `conn:wshenabled:false` declarativo desde el catálogo. La visibilidad del error se arregla en el backend y no subiendo `term:conndebug` a `info` por defecto: el gating de `blocklogger` es correcto para progreso verboso, pero "no se pudo arrancar la shell" no es progreso, y activar conndebug llenaría cada terminal de ruido. La host key NO se auto-acepta: confiar en claves desconocidas en silencio convertiría un fix de UX en un agujero MITM; es la única fricción legítima y ocurre una sola vez por host. `docs/` (Docusaurus de Wave) queda sin tocar por D-001 aunque su tabla diga que el default es `true` |
| D-030 | 2026-08-01 | **La UI es el camino primario del catálogo; el importador YAML pasa a bootstrap opcional.** Administrador de servidores en la app (`EnvEditModal` + `frontend/app/nexus/env-store.ts`): alta/edición/duplicado/borrado desde la sidebar, `NexusEnvType` extendido (description, group, user, port, identityfile, wsh, initscript), y contraseñas en el secret store del SO (`nexus_ssh_<id>` + `ssh:passwordsecretname` en connections.json), nunca en la config. El importador **preserva** los ambientes creados desde la app en vez de borrarlos, y el puerto 22 del formulario se trata como default (no se escribe `ssh:port`). Doc: [ENVIRONMENTS.md](ENVIRONMENTS.md) | El pedido era administrar los servidores desde la app y no volver a tipear una contraseña; con solo el importador, agregar un servidor obligaba a editar YAML y correr un script, y la sidebar se ocultaba con el catálogo vacío (sin sidebar no hay "+", sin "+" el primer servidor volvía al YAML). Los campos nuevos duplican a propósito lo que va a `connections.json`: `conn` es lossy (se come el `:22`, no lleva la clave) y sin ellos el editor no podría reconstruir el formulario. Las claves propias se escriben siempre, con `null` cuando no aplican, porque `SetConnectionsConfigValue` solo mergea y omitir una clave dejaría pegado el valor viejo — pasar de Contraseña a Clave seguiría mandando `ssh:passwordsecretname`; la contracara es que esa RPC no puede borrar la entrada del host, así que el borrado deja el registro inerte con nulls. El bug más caro estaba en el importador: `settings["nexus:environments"] = catalog.environments.map(...)` **borraba en silencio** todo servidor creado desde la UI en cada reimportación, lo que habría sido inaceptable; ahora es dueño solo de los ids que declara. Y escribir `ssh:port: "22"` porque el formulario muestra 22 habría pisado un `Port 2222` de `~/.ssh/config` — la misma trampa que ya motivaba no emitir `ssh:hostname`, dado que `connections.json` le gana a la config de SSH. El secreto se deriva del **id** y no del nombre visible para que renombrar no pierda la contraseña, y duplicar NO clona el secreto porque exigiría leer el valor en claro para reescribirlo con otro nombre |
| D-031 | 2026-08-01 | **Dos familias de IA, cinco accesos, un catálogo.** El catálogo único pasa a la app (`frontend/app/nexus/ai-apps.ts`) y deja de depender del importador: **Codex CLI** y **Claude CLI** (bloque `term`, con `nexus:modes` normal/⚠ permisos totales en el menú del click) más **ChatGPT**, **Claude Chat** y **Gemini** (`gemini.google.com/app`; bloque `web` genérico, uno por proveedor, partición `persist:ai-<id>`). Antigravity NO entra: es el IDE de escritorio de Google, no un chat web. `mergeAiWidgets` funde los widgets LOCALES de `widgets.json` que lanzan `claude`/`codex` dentro del botón built-in (conservando como modo extra cualquier invocación que no esté en el catálogo) y respeta como override un widget del usuario con la misma clave (`nexus-ai-cli-*`, `nexus-ai-web-*`); un agente con `connection` no se pliega. El importador hace el mismo plegado en el origen (`collapseAgents`, por herramienta + ambiente). Seguridad de los paneles: `will-attach-webview` fuerza `nodeIntegration:false`/`contextIsolation:true`/`webSecurity:true` en cualquier `<webview>`; la política de `ai-web-policy.ts` sobre cada sesión concede solo portapapeles sanitizado, pantalla completa, storage-access y micrófono; certificados siempre validados por Chromium (`-3`); un `window.open` de login se atiende con ventana emergente **dentro de la app** en la misma partición y con `sandbox:true` sin preload, el resto de los links va al navegador externo y los esquemas no-web no se abren; el user-agent de los paneles de IA omite los tokens `waveterm/`/`Electron/` | El pedido era eliminar los cuatro botones (Codex, Codex permisos completos, Claude, Claude permisos completos) y sumar los chats web. D-028 ya había implementado los `modes`, pero **la solución vivía entera en el importador**: una instalación cuyo catálogo declaraba una entrada por variante —o cuyo `widgets.json` había quedado escrito por un importador viejo— seguía mostrando cuatro botones y no había forma de arreglarlo sin re-importar. Por eso el plegado se hace ahora también en la barra, que es la superficie que el usuario mira. Los chats web NO son una vista nueva: reusan el bloque `web` para heredar gratis mover/redimensionar/maximizar/desacoplar/persistir; la identidad de cada instancia es el blockId y lo compartido es la partición. `persist:` es el único mecanismo que hace que el login sobreviva al reinicio, y una partición por proveedor evita que la cookie de uno viaje al otro. El plegado excluye los widgets con `connection` porque un agente remoto es otra herramienta: fundirlo dentro del botón local abriría el CLI en la máquina equivocada. La ventana emergente de login se deja crear a Electron (`action:"allow"` con `overrideBrowserWindowOptions`) en vez de fabricarla nosotros: así conserva la relación con su `opener`, sin la cual los OAuth que hacen `postMessage` al abridor no cierran. Mandar ese popup al navegador del sistema —el comportamiento anterior— deja la sesión afuera y el panel deslogueado para siempre. El user-agent se sanea porque Google bloquea por política los "navegadores embebidos": se omiten los tokens del envoltorio y se conservan plataforma y versión reales, que es distinto de falsear el cliente. Las URLs quedan editables sin recompilar (clave del widget en `widgets.json` gana). La primera versión de esta decisión ponía "Antigravity Chat" en lugar de Gemini por un malentendido del producto: Antigravity es el IDE agéntico (el lado de código de Gemini) y no tiene chat web, así que apuntaba al sitio del producto y habría sido un botón inútil |
| D-032 | 2026-08-01 | **Un botón por PROVEEDOR, no por superficie.** Reemplaza el layout de D-031: la barra muestra **ChatGPT**, **Claude** y **Gemini**, y el click abre un menú con las superficies de ese proveedor (chat web · CLI normal · ⚠ CLI permisos totales). Mecanismo nuevo `nexus:actions` en `widget-modes.ts`: a diferencia de `nexus:modes` —que solo puede cambiar el `cmd` de un bloque terminal— una acción lleva el `meta` completo del bloque, así un mismo botón puede abrir un `view:web` y un `view:term`. Un proveedor con una sola superficie (Gemini) no muestra menú: abre directo. Claves `nexus-ai-<proveedor>`. El plegado de `mergeAiWidgets` se extiende: `unwrapInvocation` atraviesa variables de entorno, `env`, `wsl -d X --`, `bash/sh/zsh -lc "..."`, `cmd /c` y `sudo/ssh` antes de identificar la herramienta, y los widgets `view:web` que apuntan al mismo origen que un chat del catálogo también se pliegan | El usuario seguía viendo "botones de más" con cinco accesos: dos CLIs + tres chats es correcto por superficie pero se lee como cinco herramientas cuando en realidad son tres proveedores. Agrupar por proveedor es el eje que el usuario tiene en la cabeza ("quiero Claude") y baja la barra a tres botones sin esconder ninguna capacidad: todo lo que antes era un botón ahora es un ítem del menú, a un click de distancia. `nexus:actions` y no reusar `nexus:modes` porque un modo es, por definición, una variante de comando de UNA herramienta de terminal: forzar un chat web ahí habría significado meter un `view` dentro de algo que se llama "modo", y el importador —que sigue emitiendo `nexus:modes`— habría empezado a producir entradas que no puede representar. Los dos mecanismos conviven: las acciones ganan si están, los modos siguen funcionando para lo que genera el importador. `unwrapInvocation` existe porque la detección por primer token fallaba justo en el caso del usuario: un widget que lanza el CLI a través de `wsl` o de una shell no se reconocía como el mismo agente y quedaba como botón extra — la causa real de "me siguen apareciendo botones de más" junto con el hecho de que la app instalada seguía siendo la beta.9, sin nada de D-031 |
| D-033 | 2026-08-01 | **Los tres proveedores tienen CLI: `agy` entra como agente de terminal de Gemini.** El botón **Gemini** deja de ser un acceso de un solo click y pasa a ofrecer las mismas tres superficies que los otros dos: chat web (`gemini.google.com/app`, partición `persist:ai-gemini`), **Agy CLI — Normal** (`agy`) y **⚠ Agy CLI — Permisos totales** (`agy --dangerously-skip-permissions`). `agy` se suma a `AI_CLI_AGENTS`, con lo cual `mergeAiWidgets` lo pliega igual que a `claude`/`codex`: un widget heredado de `widgets.json` que lance `agy` —o `wsl -d Ubuntu -- agy --dangerously-skip-permissions`— desaparece como botón suelto y queda como acción dentro de Gemini | La barra tenía que quedar en exactamente tres botones, uno por proveedor, sin ningún acceso duplicado ni un botón por variante de permisos. Antigravity es el producto de código de Google y su superficie de terminal es `agy`, que soporta el mismo flag de bypass que Claude Code; ponerlo como cuarto botón habría reintroducido justo el problema que D-031/D-032 vinieron a resolver. Sigue sin haber "Antigravity Chat": el IDE no tiene chat web, así que el chat del proveedor es Gemini y el CLI es `agy` — mismo proveedor, dos superficies, un botón |
